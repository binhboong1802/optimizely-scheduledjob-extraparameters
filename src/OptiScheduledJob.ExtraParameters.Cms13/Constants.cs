namespace OptiScheduledJob.ExtraParameters
{
    public class Constants
    {
        // Unchanged from the CMS 12 line on purpose: the protected module keeps the same name so an
        // upgraded site resolves the same module folder and the same routes.
        public const string ModuleName = "OptiScheduledJob.ExtraParameters";
    }
}
